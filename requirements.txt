# Índice extra de PyTorch solo-CPU. Sin esto, en el servidor Linux pip se baja
# la versión de torch con CUDA (~2,5 GB), que no cabe en los planes pequeños y
# hace que el despliegue falle por falta de disco.
--extra-index-url https://download.pytorch.org/whl/cpu

# Dependencias directas del proyecto (el resto las resuelve pip automáticamente)
Flask==3.1.3
gunicorn==26.0.0
python-dotenv==1.2.2
anthropic==0.111.0
numpy==2.4.6
sentence-transformers==5.6.0

# torch solo-CPU en el servidor Linux; en tu Mac se instala la normal (que ya es
# solo-CPU). El marcador "; sys_platform == ..." elige automáticamente según el sistema.
torch==2.12.1+cpu ; sys_platform == "linux"
torch==2.12.1 ; sys_platform == "darwin"
